name: ci(smoke): manual-smoke (manual)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Hello
        shell: bash
        run: echo "hello $GITHUB_SHA @ $(date -u)"
