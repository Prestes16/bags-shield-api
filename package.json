{
  "name": "bags-shield-api",
  "version": "0.3.8",
  "private": true,
  "type": "module",
  "engines": { "node": "20.x" },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "launch": "npm --prefix scripts run launch"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {}
}
