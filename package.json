{
  "name": "bags-shield-api",
  "version": "1.0.0",
  "description": "API Gateway for Bags Shield project on Vercel",
  "main": "api/ping.js",
  "scripts": {
    "start": "node api/ping.js",
    "build": "echo 'No build step required for CJS functions.'"
  },
  "engines": {
    "node": "20.x"
  }
}