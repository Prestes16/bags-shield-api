{
  "name": "bags-shield-api",
  "version": "1.0.0",
  "private": true,
  "description": "Bags Shield — Fase 4: API serverless (Vercel Functions, Node 20). Rotas: /api/health, /api/scan, /api/simulate, /api/apply.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "deploy": "vercel --prod --yes",
    "deploy:preview": "vercel --yes",
    "type-check": "tsc --noEmit",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.6.0",
    "vercel": "^48.1.6"
  }
}
