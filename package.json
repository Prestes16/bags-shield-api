{
  "name": "bags-shield-api",
  "private": true,
  "version": "0.3.9",
  "type": "commonjs",
  "scripts": {
    "build": "node -e \"console.log('no build step')\"",
    "vercel-build": "node -e \"console.log('no build step')\""
  },
  "engines": { "node": ">=20 <21" }
}
