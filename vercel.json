{
  "version": 2,
  "builds": [
    { "src": "api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/health", "dest": "/api/router?path=/api/health" },
    { "src": "/api/token/([^/]+)/creators", "dest": "/api/router?path=/api/token/$1/creators&mint=$1" },
    { "src": "/api/token/([^/]+)/lifetime-fees", "dest": "/api/router?path=/api/token/$1/lifetime-fees&mint=$1" }
  ]
}