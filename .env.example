# .env.example — copie para .env e preencha localmente (NÃO commitar .env)
BAGS_API_KEY=coloque_sua_api_key_aqui
# Para testes, use DEVNET (recomendado):
SOLANA_RPC_URL=https://api.devnet.solana.com
# Para mainnet, use um RPC dedicado/estável (Helius/QuickNode/etc)
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# PRIVATE_KEY em base58 (NÃO commitar).
# Se exportou um array JSON (64 números), converta para base58 (veja dicas no README).
PRIVATE_KEY=coloque_sua_secret_key_base58_aqui

# (opcional)
# SOLANA_COMMITMENT=processed
